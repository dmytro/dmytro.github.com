# A sample Guardfile
# More info at https://github.com/guard/guard#readme

# guard 'brakeman' do
#   watch('Gemfile')
#   watch('Guardfile')
# end


guard 'rspec', :version => 2 do
  watch(%r{(.+)\.textile}) { "spec"}
end

